name: test

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  macos:
    strategy:
      matrix:
        os: [macos-15, macos-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - name: Test
        run: make test
